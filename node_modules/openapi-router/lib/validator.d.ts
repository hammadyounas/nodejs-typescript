/// <reference types="express" />
import * as express from 'express';
import { OpenAPI, RequestValidationResult, ResponseValidationError, ResponseOptions } from './types';
export declare class Validator {
    private spec;
    constructor(operationId: string, spec: OpenAPI.Schema, options?: ResponseOptions);
    validateRequest(values: {
        [name: string]: any;
    }): RequestValidationResult;
    validateResponse(res: express.Response, bodyArgs: any): {
        errors: ResponseValidationError[];
        isValid: boolean;
    };
    readonly ignoreResponseErrors: boolean;
    private validateValue(value, parameterSchema);
    private getResponse(statusCode);
    private readonly operation;
    readonly parameters: OpenAPI.Parameter[];
    private readonly jsonValidator;
    private readonly ignoreInvalidHeaders;
    private readonly ignoreMissingHeaders;
    private readonly ignoreInvalidBody;
    private readonly ignoreInvalidStatus;
}
