/// <reference types="express" />
import * as express from 'express';
import { OpenAPI, ResponseOptions, RouteHandler } from './types';
export declare class Router {
    private spec;
    private app;
    private options;
    constructor(spec: OpenAPI.Schema, app: express.Express, options?: ResponseOptions);
    /** Adds a route handler for the specified OpenAPI operation */
    use(operationId: string, handler: RouteHandler): void;
    /** Adds handlers for un-implemented operations, unsupported HTTP methods, as well as a final 404 catch-all */
    addCatchAllRoutes(): void;
    private readonly handledPaths;
    private readonly operations;
}
